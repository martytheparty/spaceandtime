import { StAnimationDeferredDepsClass } from './st-animation-deferred-deps.class';

describe('StAnimationDeferredDepsClass', () => {
  it('should create an instance', () => {
    expect(new StAnimationDeferredDepsClass()).toBeTruthy();
  });
});
